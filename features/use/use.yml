schema: SimV1
name: use

includes:
  - templates
  - ../http

services:
  - name: OAuth test
    steps:
      - use: OAuth template
      - to: echo
        insert:
          - value: "{B[token]}"
      - verify:
          - value: '{REGEX["^[0-9a-fA-F]{8}-([0-9a-fA-F]{4}-){3}[0-9a-fA-F]{12}$"]}'